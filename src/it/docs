# Per Iniziare

Così, ora hai scaricato con successo Skidfuscator sul tuo computer e ti stai chiedendo come eseguirlo?

Skidfuscator viene eseguito attraverso la riga di comando, usando la CLI di Java. Vi consigliamo vivamente di familiarizzare con questo al fine di godere di un'esperienza senza soluzione di continuità Skidfuscator.

## Opzioni CLI per Skidfuscator

Semplicemente esegui `java -jar <yourSkidfuscatorFile.jar>` e ti verranno presentate varie opzioni:

![Opzioni di Skidfuscator](https://i.imgur.com/Yu70Pcq.png)


#### Il tuo jar di input
Il jar di input che vuoi offuscare con Skidfuscator.
```cli
<input>
```

#### Le tue esclusioni
Il percorso del file che contiene le tue esclusioni.
```cli
--exempt=<exempt>
```

#### Salta calcolo
**USARE QUESTO AL TUO RISCHIO PROPRIO** Salta completamente le forme critiche di calcolo come le librerie. Il supporto può essere limitato se si sceglie di utilizzare questa opzione.
```cli
`--fuckit
```

#### Le tue librerie
Il percorso alla cartella che contiene librerie / dipendenze richieste per offuscare il file jar.
```cli
--libs=<libs>
```

#### Opt out di analisi
Questa opzione non ci fornirà alcuna analisi come errori che si possono riscontrare, o statistiche di utilizzo obfuscator.
```cli
--notrack
```

#### Il tuo output
Il percorso in cui si desidera che il vostro barattolo offuscato sia output, se questa opzione è lasciata vuota, verrà output nella directory in cui Skidfuscator è stato eseguito.
```cli
--output=<output>
```

#### Calcolo fantasma
Consente di abilitare il calcolo fantasma per le classi, ancora una volta, se questa opzione è utilizzata, il supporto può essere limitato. Ti consigliamo vivamente di fornire le librerie necessarie necessarie e offuscare il tuo file di barattolo.
```cli
--phantom
```

#### Specificare il runtime
Il percorso della tua pista di java.
```cli
--runtime=<runtime>
```