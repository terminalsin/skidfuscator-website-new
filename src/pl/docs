## Wyjątek zwrotu

Zaciemnianie zwracanych wartości wyjątków to technika programistyczna stosowana w celu utrudnienia zrozumienia i analizy przepływu sterowania w programie poprzez zaciemnianie zwracanych wyników i standaryzację każdej metody tak, aby miała pustą wartość zwracaną.

W skrócie, technika ta zastępuje normalne instrukcje powrotu blokami obsługi wyjątków, które sprawiają, że przepływ sterowania programu jest mniej przewidywalny. Gdy zostanie zgłoszony wyjątek, program przeskakuje do wyznaczonego programu obsługi wyjątków, który następnie zwraca żądaną wartość lub wykonuje zamierzoną akcję. Sprawia to, że kod jest trudniejszy do odczytania ianalizy, zapewniając w ten sposób wysoki poziom ochrony przed manipulacją lub piractwem.

'''warning Ta technika zaciemniania jest wciąż eksperymentalna i może nie kompilować się pomyślnie na większych programach. Z tego powodu jest ona domyślnie wyłączona. '''

## Konfiguracja

```js
exceptionReturn {
    enabled: false
    exempt: []
}
```

## Przykłady

### Brak obfuskacji

```java
int number = callNumber();
 [...]
}

int callNumber() {
    return 1;
}
```

### Z obfuskacją
```java
try {
    callNumber();
} catch (IntNumberException ex) {
    int number = ex.getValue();
}

[...]

void callNumber() {
    throw new IntNumberException(1);
}
```




